fastapi==0.111.0
uvicorn==0.29.0
huggingface-hub==0.36.2
pinecone-client==3.2.2
supabase==2.31.0
pytesseract==0.3.10
Pillow==10.3.0
PyMuPDF==1.24.5
pydantic-settings==2.2.1
textstat==0.7.3
python-multipart==0.0.9

# NOTE: the one-time corpus ingest (scripts/ingest_corpus.py) needs extra heavy
# deps (datasets, sentence-transformers, torch). Those live in
# requirements-ingest.txt and are intentionally NOT installed in the deployed
# backend — production only embeds the query, via the HF Inference API.
